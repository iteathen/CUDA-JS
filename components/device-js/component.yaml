{
  "schema_version": 1,
  "component": {
    "id": "runtime.device-js",
    "name": "Restricted Device-JS frontend",
    "status": "accepted-spec0013-single-unit-windows-qualified-plus-spec0028-portable-only",
    "product_area": "components",
    "path": "components/device-js",
    "purpose": "Own the restricted consumer-neutral Device-JS syntax/type/helper and typed leaf-library composition contracts, deterministic identities, and private CUDA C++ lowering without owning native compiler or Driver resources.",
    "public_contracts": ["index.mjs"],
    "allowed_dependencies": ["node-standard-library", "acorn-8.15.0-syntax-only", "runtime.cuda-target"],
    "forbidden_dependencies": ["cuda-driver", "node-ffi", "nvrtc-resource-ownership", "nvjitlink-resource-ownership", "consumer-domain-semantics", "search-semantics", "public-generated-cuda-source"],
    "validation": ["node --test components/device-js/test/translator.test.mjs components/device-js/test/strict-contract.test.mjs components/device-js/test/device-library.test.mjs", "npm run f8:unit", "npm run f8:portable", "npm run f8:native"],
    "owner": "runtime.device-js",
    "lifecycle": "Pure JavaScript preprocessing only; owns no native resource. Generated CUDA source is private transient input to the existing CompilerActor through runtime.facade.",
    "design": {
      "owned_invariant": "Only accepted SPEC-0013 single units, bounded atomic/publication children, and SPEC-0028 typed leaf-library compositions reach CUDA lowering; identities and lowering are deterministic by exact source, metadata, compile/composition facts, parser version, helper semantics, and code-unit function order.",
      "public_ports": ["translateDeviceProgram", "translateDeviceLibrary", "DEVICE_JS_LIBRARY_CONTRACT"],
      "injected_dependencies": ["canonical source text", "explicit function/type metadata", "typed compile options"],
      "adapter_boundaries": ["pinned Acorn syntax-only parser", "runtime.facade compileDeviceProgram/compileDeviceLibrary bridges"],
      "simplest_sufficient_total_system": "One pinned commodity syntax parser, one fail-closed semantic validator/lowerer, one deterministic normalization boundary, and reuse of the existing compiler owner."
    }
  }
}

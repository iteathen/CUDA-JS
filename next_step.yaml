{
  "schema_version": 61,
  "updated": "2026-09-03",
  "status": "tensor_physical_boundary_handoff",
  "objective": "Keep CUDA-JS consumer-neutral while the active portfolio refactor removes copied CUDA/provider facts from CUDA-JS-Tensor. The alpha.17 compatibility projection supplies lower prepared-DAG and Device-JS ceilings without introducing a launch resolver, universal preparation transaction, provider registry, or consumer semantics.",
  "package_candidate": "cuda-js@0.1.0-alpha.17",
  "completed_refactor_nodes": [
    "#178 provider capability/admission truth: existing SPEC-0023/SPEC-0029 model retained; workspace alignment and unsupported fallback truth publicly usable",
    "#179 cuBLASLt borrower lifecycle: one runtime-owned underlying provider with independent public borrowers; no generic provider-sharing model",
    "#180 executable-preparation decision: no new higher-level transaction; existing compiler/artifact/module/function/provider/prepared-DAG bricks remain the correct composition seam",
    "#181 launch-resolution decision: no new resolver; explicit grid/block remains canonical and upper selected physical-profile geometry remains upper-owned",
    "#186 finite public compatibility projection: SPEC-0020 prepared-DAG ceilings and Device-JS per-function parameter ceiling are exposed through immutable CUDA_JS_COMPATIBILITY"
  ],
  "current_focus": {
    "repository": "iteathen/CUDA-JS-Tensor",
    "issue": 45,
    "name": "physical lowering migration",
    "next_action": "Advance Tensor to the protected alpha.17 CUDA-JS revision. Consume CUDA_JS_COMPATIBILITY preparedOperationDagLimits and deviceJsLimits instead of copied lower ceilings; preserve Tensor-owned mathematics, liveness, access/dependency meaning, workspace budgets, blockSize and logical-work-to-grid mapping; preserve the accepted explicit ptx/lto-ir artifact-family choice while validating it against the public lower compatibility set. Continue using the existing public compile/module/function/prepareOperationDag bricks and reverse-order adapter cleanup.",
    "exit": "Tensor #45 is protected-integrated with no copied CUDA-JS provider lifecycle, provider facts, prepared-DAG ceilings, or Device-JS parameter ceiling; no second runtime lifecycle or speculative lower abstraction exists."
  },
  "after_current_focus": [
    "CUDA-JS-Tensor #40 final execution-ownership reconciliation",
    "CUDA-MCGS #193 execution-package/CUDA-JS adapter ownership audit",
    "CUDA-MCGS #125 only after all independent prerequisites are satisfied"
  ],
  "parallel_external_lanes": [
    "Native Linux x86-64 qualification remains contributor/external evidence work on a directly exposed physical NVIDIA host.",
    "Exact native promotion for other already-implemented capabilities remains independent and does not block the Tensor ownership refactor unless a selected profile specifically requires it."
  ],
  "hard_constraints": [
    "Do not add Tensor, NN, MCGS, media, data, ray, graph-analysis or other consumer semantics to CUDA-JS.",
    "Do not replace explicit expert launch geometry with a mandatory logical-work resolver.",
    "Do not create a universal PreparedExecutable/GpuProgram/provider registry merely to reduce adapter code.",
    "Do not treat portable/mock evidence as native qualification.",
    "Do not modify CUDA-MCGS #122 without explicit project-owner authorization."
  ]
}

{
  "schema_version": 64,
  "updated": "2026-09-04",
  "status": "current_state_freshness_recurrence_prevention",
  "objective": "Complete #156 by reconciling the protected alpha.18 state and adding a small mechanical current-state contract so package/capability/execution-seam declarations cannot silently drift after later protected integration.",
  "package_candidate": "cuda-js@0.1.0-alpha.18",
  "public_api_schema": 1,
  "live_state_policy": {
    "protected_commit_identity": "remote-readback-only",
    "package_identity_owner": "package.json",
    "public_compatibility_owner": "packaging/compatibility-manifest.json",
    "execution_seam_owner": "STATUS.md + next_step.yaml",
    "qualification_owner": "docs/HARDWARE_SUPPORT.md + docs/NODE_SUPPORT.md + qualification registries/evidence",
    "issue_owner": "durable obligations, explicit blocked gates and concrete evidence cells; never live SHA dashboards"
  },
  "recorded_protected_input": {
    "main": "49a2f77d2c8364d67030fbc1c2e870e58e70d334",
    "tree": "b67890e2499f04ab3b81b8f4a72dab38a5281c7e",
    "meaning": "Exact protected base from which the #156 recurrence-prevention transaction was constructed. It remains transaction provenance after integration and is not a self-updating current-main field."
  },
  "completed_dependencies": [
    "#193 / PR #197 protected-integrated the lower-owned 256-byte ordinary base-allocation compatibility projection and alpha.18 package identity",
    "#178/#179/#180/#181/#186 lower-boundary ownership work completed",
    "CUDA-MCGS #125 / PR #210 protected-integrated the production integration.cuda-js adapter through public cuda-js@0.1.0-alpha.18 contracts",
    "Protected alpha.18 required verify/schema/node-compatibility and current-head CodeQL are green"
  ],
  "durable_authority_markers": [
    "CJS-F0",
    "CJS-F1A",
    "ADR-0002-node-ffi-first-host-binding.md",
    "ADR-0007"
  ],
  "cross_repo_ownership": {
    "cuda_nn": {
      "id": "CJS-CUDA-NN-EXTERNAL",
      "authority": "ADR-0007",
      "disposition": "Reusable NN/model/inference/autodiff/training semantics are external to CUDA-JS and belong to iteathen/cuda-nn. Generic Tensor mathematics/planning belongs to CUDA-JS-Tensor."
    },
    "cuda_mcgs": {
      "issue": 32,
      "disposition": "CUDA-MCGS owns search semantics and integration.cuda-js translation; CUDA-JS owns generic lower runtime/mechanism contracts and exact lower compatibility facts."
    }
  },
  "current_focus": {
    "repository": "iteathen/CUDA-JS",
    "issue": 156,
    "name": "current-state and issue-obligation freshness recurrence prevention",
    "state": "governance implementation from exact protected alpha.18 base",
    "next_action": "Replace stale live-dashboard prose in agent entry points, reconcile STATUS.md/next_step.yaml to protected alpha.18, add and qualify the current-state contract validator, complete exact-head author review, then stop for fresh protected-integration authorization.",
    "exit": "One exact #156 head is green and completely reviewed; package/compatibility/status/next-step agreement is mechanically checked; agent entry points no longer carry live package/SHA snapshots; issue disposition/current-state ownership rules are documented; protected integration remains gated on fresh exact-tuple authorization."
  },
  "parallel_evidence": [
    "#4 native Ubuntu 24.04 x86-64 qualification remains runner-ready but requires a directly exposed physical NVIDIA GPU; hosted CI/VM/WSL/mock evidence cannot close it.",
    "#68 unaffiliated private-vulnerability reporter -> advisory -> maintainer proof remains external operational evidence.",
    "#157 remains an independent public Device-JS erf helper consumer lane for CUDA-JS-Tensor."
  ],
  "after_current_focus": [
    "Read back the resulting protected CUDA-JS commit/tree after #156 integration.",
    "Perform a fresh #32 assess/research pass against that exact CUDA-JS revision and the exact protected CUDA-MCGS consumer revision produced after its #211/#212 governance reconciliation.",
    "Run the smallest exact compatible-pair/native capsule entirely through CUDA-MCGS integration.cuda-js and public CUDA-JS contracts; freeze Node/ABI/OS/Driver/toolkit/provider/GPU/target/artifact identity and preserve pressure/failure/health/cleanup truth.",
    "If #32 demonstrates a generic lower gap, route it to the owning CUDA-JS capability before any consumer-local native/private workaround.",
    "Run #4 unchanged when an accepted native Ubuntu 24.04 x86-64 physical-NVIDIA host becomes available; preserve the complete exact-profile evidence bundle and promote only the tested cell."
  ],
  "current_state_contract": {
    "validator": "scripts/current-state-contract.mjs",
    "tests": "scripts/current-state-contract.test.mjs",
    "gate": "scripts/verify-docs.sh",
    "checks": [
      "package.json name/version equals next_step package_candidate",
      "package.json name/version and public API schema agree with packaging/compatibility-manifest.json",
      "STATUS.md states the same package and current issue",
      "recorded protected input is explicitly provenance, not a self-referential current-main declaration",
      "durable agent entry points contain live-state routing and do not reintroduce the retired live-dashboard headings",
      "forbidden live-SHA field names such as current_main/current_main_tree are rejected"
    ]
  },
  "issue_disposition_rule": [
    "Open: concrete actionable implementation/governance work, explicit blocked gate with a known unblock condition, or concrete external qualification/evidence cell.",
    "Completed: implementation/obligation complete; later unspecified profile evidence moves to qualification campaigns rather than keeping the implementation issue open.",
    "Not planned: dormant possibility with no active consumer/profile/measurement; this is administrative scheduling, not architectural rejection.",
    "Superseded/duplicate: retain exact successor/provenance and do not let the old issue remain a competing current-state owner."
  ],
  "hard_constraints": [
    "No protected integration without exact-head qualification, complete review and fresh exact-tuple authorization.",
    "Do not use agent docs, issues, plans or historical comments as live package/SHA/capability dashboards.",
    "Do not treat portable/mock evidence as native CUDA qualification.",
    "Do not add consumer semantics, raw pointers, private/deep imports or native bypasses to satisfy #32 or #4.",
    "Generic lower gaps route to CUDA-JS; generic Tensor gaps route to CUDA-JS-Tensor; reusable NN/model gaps route to cuda-nn."
  ]
}

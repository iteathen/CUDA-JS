# CUDA-JS Status

**Status:** Active operational state

**Updated:** 2026-09-04

## Current package and capability projection

```text
package:                     cuda-js@0.1.0-alpha.18
public API schema:           1
host source model:           JavaScript/ESM, Worker-owned Node FFI
exact Node evidence baseline: Node 26.7.0
native Linux x86-64:         testing-unconfirmed / not-qualified
production support:          no
performance claims:          none beyond exact recorded evidence
```

`package.json` owns package identity. `packaging/compatibility-manifest.json` owns the immutable public capability/compatibility projection. Exact protected branch/commit/tree identity is read from GitHub when required; it is not maintained here as a self-referential live-SHA field.

The recorded protected CUDA-JS input for this state-refresh transaction is `main@dd41ce0693c91aff867a9db9ad4f11b507eeb38b`, tree `b331ddc3cadb48003ba86d7b920e7b6cb34606a1`. After this docs-only reconciliation integrates, that value remains transaction provenance rather than a self-updating current-main declaration.

## Protected alpha.18 state

#156 / PR #199 is protected-integrated. Its current-state governance LEGO routes live state to existing owners, rejects self-referential live-main fields and retired agent dashboards, and checklist-enforces issue disposition/reconciliation without creating a second technical authority. #200 / PR #201 is also protected-integrated and activated CUDA-JS #32 as the current cross-repository evidence seam. Protected `verify`/`schema`, node-compatibility and CodeQL remain green for the alpha.18 protected state.

The ordinary CUDA-JS base-allocation minimum alignment remains lower-owned by `runtime.memory` and publicly projected as 256 bytes. The package retains Device-JS, opaque operation lifecycle, publication mailboxes, device publication, explicit launch geometry, finite compatibility ceilings, compiler/linker/module/function/resource ownership, and the existing Windows x64 exact-profile evidence. Generic Tensor mathematics remains in CUDA-JS-Tensor; reusable NN/model semantics remain in `iteathen/cuda-nn`; search semantics remain in CUDA-MCGS.

Native Linux CUDA remains unqualified. Portable/readiness/package evidence is not native support, successful execution is not a broad compatibility claim, and no performance or production-stability claim is promoted here.

### Durable architecture and evidence anchors

These are provenance/ownership anchors, not live implementation or support dashboards. `DriverActor` remains the Worker/context/native-resource owner. `CJS-F1B` remains the generated CUDA ABI-fact and independent layout-evidence anchor; `CJS-F2W` remains the accepted Windows x64 Driver/bootstrap evidence anchor; `CJS-F7W` remains the retained Windows platform-hardening/property/lifecycle evidence anchor. Their historical evidence remains exact to recorded revisions/profiles and is not silently requalified by this transaction.

### External CUDA-NN ownership

**External CUDA-NN ownership** remains governed by ADR-0007. Reusable NN/model/inference/autodiff/training semantics belong to independent `iteathen/cuda-nn`, while generic Tensor mathematics/planning belongs to CUDA-JS-Tensor. The historical bootstrap provenance anchor `iteathen/cuda-nn@7d7854697049db38e4a0670b80df9d600cd442c3` remains audit evidence only; those reusable NN semantics **no longer belong to a future publish unit in this repository**.

## Current focus — #32 exact CUDA-MCGS compatible-pair qualification

**#32 — CUDA-MCGS compatible-pair qualification through public CUDA-JS** remains the current P0 cross-repository assess/research/evidence seam.

The exact protected consumer input now recorded for this assessment is CUDA-MCGS `main@85ee5d261fa50a4933e091cc993c60867158eea1`, tree `07d8ba12e475ce78b9a65c5d9e891296d7e3fd4f`. This is the protected post-#215/#218 consumer state actually available for the pair. CUDA-MCGS #125 / PR #210 provides production `integration.cuda-js`; #215 / PR #217 adds explicit terminal Output host delivery through the accepted Program Package/Search Program path; #218 / PR #219 reconciles that protected state while keeping #32 active. Protected post-#218 documentation/required-verify, Windows+Ubuntu runtime-adapter and CodeQL checks passed.

The protected adapter translates accepted execution-package meaning through the public CUDA-JS namespace only. Its terminal delivery validates an explicit package declaration before lower mutation and uses public CUDA-JS asynchronous D2H only after completed operation state. That closes the prior first-consumer package/adapter connection gap without creating a CUDA-JS lower capability or an arbitrary resource-read surface.

Fresh #32 evidence must freeze one truthful exact pair immediately before physical execution. The historical #125 portable fixture pins the then-protected CUDA-JS revision `49a2f77d2c8364d67030fbc1c2e870e58e70d334`; it is conformance provenance, not permission to mislabel a later checkout. Likewise, the MCGS pair identity must be the protected revision actually executed, not an older implementation-only SHA.

The first selected pair reuses CUDA-MCGS `tool.search-compiler` to produce accepted product-neutral Search Program/execution-package meaning and `integration.cuda-js` to realize it. CUDA-JS contributes only existing public mechanisms unless that real consumer demonstrates a missing generic lower capability. No CUDA-MCGS private/deep import, raw-handle path, generated-artifact inspection, duplicated lower validator, or direct CUDA/PTX/FFI bypass is permitted.

The #125 portable fake is not a physical workload oracle: its synthetic 16-byte `u32` output is paired with a 64-thread `globalX` write and must not be promoted unchanged to native execution. The native pair must use safe finite bounds and launch geometry derived from the accepted MCGS owner path.

The smallest first pair remains **terminal-only Output**. Accepted Output semantics permit one-shot terminal publication and bounded asynchronous host delivery; protected #215 now carries that meaning end-to-end. Live observation, long-lived sideband, RDC breadth, multi-stream work, multi-GPU and Tensor facilities are not prerequisites unless the selected accepted profile proves otherwise.

## Parallel evidence — #4 native Linux

#4 remains an independent external evidence cell. The `linux-native-x64` hardware profile is repository `runner-ready` with zero missing capsules, but acceptance requires native Ubuntu 24.04 x86-64 with a directly exposed physical NVIDIA GPU, exact Node v26.7.0, and the required Driver/toolkit/compiler providers. VM, WSL, container, GitHub-hosted CI, mock, portable, or source-readiness evidence cannot close #4.

A passing #32 pair on another OS/profile does not automatically close #4; a passing #4 generic lower chain does not by itself prove CUDA-MCGS #32. Evidence may be composed only when the exact executed profile and required observations overlap truthfully.

## Open capability pressure

#198 remains conditional generic lower pressure for bounded host-written/device-read publication payloads while a long-lived operation is pending. It is not predeclared as a #32 blocker. If the selected accepted MCGS pair actually requires that behavior, #32 must stop and route the demonstrated lower requirement through #198 before any consumer-local workaround.

## Claim and integration limits

#157 remains an independent CUDA-JS-Tensor consumer lane. #68 remains an external operational security-evidence lane. CUDA-MCGS #109 remains independent public-surface work and CUDA-MCGS #123 remains downstream external-consumer acceptance.

Protected integration still requires exact-head qualification, complete review, exact base/tree validation and protected read-back. Standing user authorization is active for this workstream, so those gates must not be replaced by an authorization-only stop.
